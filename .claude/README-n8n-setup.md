# 🤖 Настройка n8n для TARS

## Что это?
Этот файл содержит инструкции по подключению TARS к вашему n8n instance через MCP-сервер для автоматизации бизнес-процессов.

## 📋 Шаги настройки

### 1. Получите API ключ из n8n

1. Откройте ваш n8n instance (например, https://your-n8n.app)
2. Перейдите в **Settings** (Настройки)
3. Выберите **API**
4. Нажмите **Create new API key** (Создать новый API ключ)
5. Скопируйте созданный ключ

### 2. Заполните конфигурацию

Откройте файл **n8n-config.json** в этой папке и замените:
- `YOUR_N8N_INSTANCE_URL` → URL вашего n8n (например: `https://your-n8n.app` или `http://localhost:5678`)
- `PASTE_YOUR_N8N_API_KEY_HERE` → ваш API ключ из шага 1

### 3. Альтернативно: использование .env файла

Если хотите использовать .env файл:
1. Скопируйте `.env.template` как `.env`
2. Заполните переменные `N8N_API_URL` и `N8N_API_KEY`

## 🔐 Безопасность

⚠️ **ВАЖНО**: Файлы с API ключами (.env, n8n-config.json) содержат конфиденциальные данные!
- Не публикуйте их в публичных репозиториях
- Добавьте `.env` в `.gitignore`

## ✅ После настройки

После заполнения конфигурации TARS сможет:
- ✅ Создавать автоматизации в n8n
- ✅ Управлять workflow
- ✅ Тестировать и деплоить интеграции
- ✅ Мониторить выполнение задач

## 📞 Поддержка

Если возникли проблемы - сообщите мне, и я помогу!

---
**TARS** - ваш AI-ассистент по развитию бизнеса 🚀
