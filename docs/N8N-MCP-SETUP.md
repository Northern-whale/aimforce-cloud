# 🚀 N8N-MCP Server - Руководство по настройке

## ✅ Что было сделано

### 1. Установка Node.js
- **Версия Node.js:** v25.6.1
- **Версия npm:** 11.9.0
- Установлено через Homebrew

### 2. Настройка N8N-MCP сервера
- Создан файл конфигурации: `.claude/settings.local.json`
- Настроен MCP сервер с отключенной телеметрией
- Использован метод **npx** (без необходимости глобальной установки)

## 📋 Текущая конфигурация

Файл: [.claude/settings.local.json](../.claude/settings.local.json)

```json
{
  "mcpServers": {
    "n8n-mcp": {
      "command": "npx",
      "args": ["-y", "n8n-mcp"],
      "env": {
        "MCP_MODE": "stdio",
        "LOG_LEVEL": "error",
        "DISABLE_CONSOLE_OUTPUT": "true",
        "N8N_MCP_TELEMETRY_DISABLED": "true"
      }
    }
  }
}
```

## 🎯 Что дает N8N-MCP?

N8N-MCP предоставляет мне доступ к **1,084 узлам n8n**:
- 537 основных узлов
- 547 узлов сообщества

### Возможности:
- 🔄 Создание автоматизированных workflow
- 🔌 Интеграция с сотнями сервисов
- 🤖 Построение сложных автоматизаций
- 📊 Обработка данных и API
- 🔔 Настройка уведомлений и триггеров

## 🔧 Дополнительные настройки

### Если у вас есть запущенный N8N сервер

Добавьте в конфигурацию:

```json
{
  "mcpServers": {
    "n8n-mcp": {
      "command": "npx",
      "args": ["-y", "n8n-mcp"],
      "env": {
        "MCP_MODE": "stdio",
        "LOG_LEVEL": "error",
        "DISABLE_CONSOLE_OUTPUT": "true",
        "N8N_MCP_TELEMETRY_DISABLED": "true",
        "N8N_API_URL": "https://your-n8n-instance.com",
        "N8N_API_KEY": "your-api-key"
      }
    }
  }
}
```

### Альтернативные методы установки

#### Docker
```json
{
  "mcpServers": {
    "n8n-mcp": {
      "command": "docker",
      "args": [
        "run", "-i", "--rm", "--init",
        "-e", "MCP_MODE=stdio",
        "-e", "LOG_LEVEL=error",
        "-e", "DISABLE_CONSOLE_OUTPUT=true",
        "ghcr.io/czlonkowski/n8n-mcp:latest"
      ]
    }
  }
}
```

## ⚠️ Важные замечания

1. **Безопасность:** Никогда не редактируйте production-рабочие процессы напрямую через AI
2. **Резервные копии:** Всегда создавайте копию перед изменениями
3. **Переменная MCP_MODE:** Обязательна для корректной работы
4. **Перезапуск:** После изменения конфигурации перезапустите Claude Code

## 📚 Полезные ссылки

- [N8N-MCP GitHub](https://github.com/czlonkowski/n8n-mcp)
- [N8N MCP Integration Guide](https://www.leanware.co/insights/n8n-mcp-integration)
- [N8N Community Forum - MCP](https://community.n8n.io/t/provide-and-use-model-context-protocol/63799)

## 🎉 Следующие шаги

После перезапуска Claude Code я буду иметь доступ к инструментам N8N и смогу:
- Создавать workflows для автоматизации
- Интегрировать различные сервисы
- Помогать вам реализовывать профессиональные идеи
- Строить сложные автоматизации

---

**Дата настройки:** 14 февраля 2026
**Статус:** ✅ Готов к использованию после перезапуска Claude Code
